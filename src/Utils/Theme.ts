import {
    createTheme,
} from '@mui/material/styles';
import {
    purple
} from '@mui/material/colors';

export default createTheme({
    palette: {
        primary: {
            main: purple[700],
        },
    },
});